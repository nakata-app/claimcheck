## What this changes

<!-- one-paragraph summary; link to a tracking issue if there is one -->

## How it was tested

<!-- pytest output or a manual repro -->

## Checklist

- [ ] `ruff check claimcheck tests` is clean
- [ ] `mypy --strict claimcheck` is clean
- [ ] `pytest -q` passes locally
- [ ] CHANGELOG entry added (under `[Unreleased]`)
- [ ] If this changes behaviour that really lives upstream
      (`adaptmem` or `halluguard`), the upstream PR is linked
- [ ] New examples are runnable end-to-end without API keys
